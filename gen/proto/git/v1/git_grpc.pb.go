// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: proto/git/v1/git.proto

// Internal Git storage transport (ADR-0004, SPEC-0015).
//
// This is never a browser-facing or untrusted-client API. Smart-HTTP and SSH front doors
// authenticate callers and translate the Git protocol into these streams (T-0011); git-storaged
// independently resolves the tenant-scoped handle and asks the PDP before it starts Git.

package gitv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	GitStorage_UploadPack_FullMethodName    = "/gitsaas.git.v1.GitStorage/UploadPack"
	GitStorage_ReceivePack_FullMethodName   = "/gitsaas.git.v1.GitStorage/ReceivePack"
	GitStorage_MergeRef_FullMethodName      = "/gitsaas.git.v1.GitStorage/MergeRef"
	GitStorage_ImportRefs_FullMethodName    = "/gitsaas.git.v1.GitStorage/ImportRefs"
	GitStorage_SetProtection_FullMethodName = "/gitsaas.git.v1.GitStorage/SetProtection"
)

// GitStorageClient is the client API for GitStorage service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type GitStorageClient interface {
	// UploadPack carries an unmodified git-upload-pack exchange for clone and fetch.
	UploadPack(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[UploadPackRequest, UploadPackResponse], error)
	// ReceivePack carries an unmodified git-receive-pack exchange for push.
	ReceivePack(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[ReceivePackRequest, ReceivePackResponse], error)
	// MergeRef moves one exact branch ref to an existing revision on behalf of an
	// authorized merge (SPEC-0019). It is the only route by which a ref changes
	// without a Git protocol exchange, and it exists so Code Review can complete a
	// merge through a contract boundary instead of against Git storage.
	//
	// It is not a general ref-write API: it moves one ref, it names no source
	// repository or path, it carries no pack bytes, and it cannot create or delete
	// a repository. git-storaged asks the PDP itself before applying it, exactly as
	// it does for a push — a caller that has already been allowed by its own PEP is
	// still not trusted to assert that here.
	MergeRef(ctx context.Context, in *MergeRefRequest, opts ...grpc.CallOption) (*MergeRefResponse, error)
	// ImportRefs fetches a source repository's refs and tags into this repository
	// on behalf of an authorized import (SPEC-0011, T-0018).
	//
	// It is the import path's git phase: the caller has already been PDP-authorized
	// by Code Review's ImportService; storage asks the PDP again with its own
	// server-derived context, then runs `git fetch` from the source URL and
	// publishes RefUpdated for every ref that moved. The fetch is acknowledged
	// through the same durability path as a push — the primary and one in-sync
	// replica must both hold the objects before the refs are announced (ADR-0016).
	//
	// The source URL and token are request-only secrets. They are never stored,
	// never logged, and never copied into an event, audit record, or error.
	ImportRefs(ctx context.Context, in *ImportRefsRequest, opts ...grpc.CallOption) (*ImportRefsResponse, error)
	// SetProtection delivers one exact-ref branch-protection rule from Code Review
	// to this storage node (SPEC-0019 AC7).
	//
	// Code Review owns the rules and has already PDP-authorized the change; the
	// rule arrives here so the receive-pack path can derive server-side context
	// for the PDP before it accepts a direct ref update. It is the cross-process
	// counterpart of BranchProtectionChanged: when Code Review and git-storaged
	// share a process the event is enough, and when they do not, this RPC is the
	// route by which the rule reaches the node that enforces direct pushes.
	//
	// Storage asks the PDP itself, exactly as for every other ref-affecting
	// operation — a caller that has already been allowed by its own PEP is still
	// not trusted to assert the rule into effect.
	SetProtection(ctx context.Context, in *SetProtectionRequest, opts ...grpc.CallOption) (*SetProtectionResponse, error)
}

type gitStorageClient struct {
	cc grpc.ClientConnInterface
}

func NewGitStorageClient(cc grpc.ClientConnInterface) GitStorageClient {
	return &gitStorageClient{cc}
}

func (c *gitStorageClient) UploadPack(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[UploadPackRequest, UploadPackResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &GitStorage_ServiceDesc.Streams[0], GitStorage_UploadPack_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[UploadPackRequest, UploadPackResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GitStorage_UploadPackClient = grpc.BidiStreamingClient[UploadPackRequest, UploadPackResponse]

func (c *gitStorageClient) ReceivePack(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[ReceivePackRequest, ReceivePackResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &GitStorage_ServiceDesc.Streams[1], GitStorage_ReceivePack_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ReceivePackRequest, ReceivePackResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GitStorage_ReceivePackClient = grpc.BidiStreamingClient[ReceivePackRequest, ReceivePackResponse]

func (c *gitStorageClient) MergeRef(ctx context.Context, in *MergeRefRequest, opts ...grpc.CallOption) (*MergeRefResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(MergeRefResponse)
	err := c.cc.Invoke(ctx, GitStorage_MergeRef_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gitStorageClient) ImportRefs(ctx context.Context, in *ImportRefsRequest, opts ...grpc.CallOption) (*ImportRefsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ImportRefsResponse)
	err := c.cc.Invoke(ctx, GitStorage_ImportRefs_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gitStorageClient) SetProtection(ctx context.Context, in *SetProtectionRequest, opts ...grpc.CallOption) (*SetProtectionResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SetProtectionResponse)
	err := c.cc.Invoke(ctx, GitStorage_SetProtection_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GitStorageServer is the server API for GitStorage service.
// All implementations must embed UnimplementedGitStorageServer
// for forward compatibility.
type GitStorageServer interface {
	// UploadPack carries an unmodified git-upload-pack exchange for clone and fetch.
	UploadPack(grpc.BidiStreamingServer[UploadPackRequest, UploadPackResponse]) error
	// ReceivePack carries an unmodified git-receive-pack exchange for push.
	ReceivePack(grpc.BidiStreamingServer[ReceivePackRequest, ReceivePackResponse]) error
	// MergeRef moves one exact branch ref to an existing revision on behalf of an
	// authorized merge (SPEC-0019). It is the only route by which a ref changes
	// without a Git protocol exchange, and it exists so Code Review can complete a
	// merge through a contract boundary instead of against Git storage.
	//
	// It is not a general ref-write API: it moves one ref, it names no source
	// repository or path, it carries no pack bytes, and it cannot create or delete
	// a repository. git-storaged asks the PDP itself before applying it, exactly as
	// it does for a push — a caller that has already been allowed by its own PEP is
	// still not trusted to assert that here.
	MergeRef(context.Context, *MergeRefRequest) (*MergeRefResponse, error)
	// ImportRefs fetches a source repository's refs and tags into this repository
	// on behalf of an authorized import (SPEC-0011, T-0018).
	//
	// It is the import path's git phase: the caller has already been PDP-authorized
	// by Code Review's ImportService; storage asks the PDP again with its own
	// server-derived context, then runs `git fetch` from the source URL and
	// publishes RefUpdated for every ref that moved. The fetch is acknowledged
	// through the same durability path as a push — the primary and one in-sync
	// replica must both hold the objects before the refs are announced (ADR-0016).
	//
	// The source URL and token are request-only secrets. They are never stored,
	// never logged, and never copied into an event, audit record, or error.
	ImportRefs(context.Context, *ImportRefsRequest) (*ImportRefsResponse, error)
	// SetProtection delivers one exact-ref branch-protection rule from Code Review
	// to this storage node (SPEC-0019 AC7).
	//
	// Code Review owns the rules and has already PDP-authorized the change; the
	// rule arrives here so the receive-pack path can derive server-side context
	// for the PDP before it accepts a direct ref update. It is the cross-process
	// counterpart of BranchProtectionChanged: when Code Review and git-storaged
	// share a process the event is enough, and when they do not, this RPC is the
	// route by which the rule reaches the node that enforces direct pushes.
	//
	// Storage asks the PDP itself, exactly as for every other ref-affecting
	// operation — a caller that has already been allowed by its own PEP is still
	// not trusted to assert the rule into effect.
	SetProtection(context.Context, *SetProtectionRequest) (*SetProtectionResponse, error)
	mustEmbedUnimplementedGitStorageServer()
}

// UnimplementedGitStorageServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedGitStorageServer struct{}

func (UnimplementedGitStorageServer) UploadPack(grpc.BidiStreamingServer[UploadPackRequest, UploadPackResponse]) error {
	return status.Errorf(codes.Unimplemented, "method UploadPack not implemented")
}
func (UnimplementedGitStorageServer) ReceivePack(grpc.BidiStreamingServer[ReceivePackRequest, ReceivePackResponse]) error {
	return status.Errorf(codes.Unimplemented, "method ReceivePack not implemented")
}
func (UnimplementedGitStorageServer) MergeRef(context.Context, *MergeRefRequest) (*MergeRefResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method MergeRef not implemented")
}
func (UnimplementedGitStorageServer) ImportRefs(context.Context, *ImportRefsRequest) (*ImportRefsResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method ImportRefs not implemented")
}
func (UnimplementedGitStorageServer) SetProtection(context.Context, *SetProtectionRequest) (*SetProtectionResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method SetProtection not implemented")
}
func (UnimplementedGitStorageServer) mustEmbedUnimplementedGitStorageServer() {}
func (UnimplementedGitStorageServer) testEmbeddedByValue()                    {}

// UnsafeGitStorageServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to GitStorageServer will
// result in compilation errors.
type UnsafeGitStorageServer interface {
	mustEmbedUnimplementedGitStorageServer()
}

func RegisterGitStorageServer(s grpc.ServiceRegistrar, srv GitStorageServer) {
	// If the following call pancis, it indicates UnimplementedGitStorageServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&GitStorage_ServiceDesc, srv)
}

func _GitStorage_UploadPack_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(GitStorageServer).UploadPack(&grpc.GenericServerStream[UploadPackRequest, UploadPackResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GitStorage_UploadPackServer = grpc.BidiStreamingServer[UploadPackRequest, UploadPackResponse]

func _GitStorage_ReceivePack_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(GitStorageServer).ReceivePack(&grpc.GenericServerStream[ReceivePackRequest, ReceivePackResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GitStorage_ReceivePackServer = grpc.BidiStreamingServer[ReceivePackRequest, ReceivePackResponse]

func _GitStorage_MergeRef_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(MergeRefRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GitStorageServer).MergeRef(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GitStorage_MergeRef_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GitStorageServer).MergeRef(ctx, req.(*MergeRefRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _GitStorage_ImportRefs_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ImportRefsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GitStorageServer).ImportRefs(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GitStorage_ImportRefs_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GitStorageServer).ImportRefs(ctx, req.(*ImportRefsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _GitStorage_SetProtection_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SetProtectionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GitStorageServer).SetProtection(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GitStorage_SetProtection_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GitStorageServer).SetProtection(ctx, req.(*SetProtectionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// GitStorage_ServiceDesc is the grpc.ServiceDesc for GitStorage service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var GitStorage_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gitsaas.git.v1.GitStorage",
	HandlerType: (*GitStorageServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "MergeRef",
			Handler:    _GitStorage_MergeRef_Handler,
		},
		{
			MethodName: "ImportRefs",
			Handler:    _GitStorage_ImportRefs_Handler,
		},
		{
			MethodName: "SetProtection",
			Handler:    _GitStorage_SetProtection_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "UploadPack",
			Handler:       _GitStorage_UploadPack_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
		{
			StreamName:    "ReceivePack",
			Handler:       _GitStorage_ReceivePack_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "proto/git/v1/git.proto",
}

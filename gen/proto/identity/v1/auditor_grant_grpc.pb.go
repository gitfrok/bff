// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: proto/identity/v1/auditor_grant.proto

// Identity & Access's auditor grant surface (SPEC-0033, T-0027, PRD PR-18).
//
// An external auditor must be able to read evidence and nothing else. A grant
// is a DISTINCT principal shape, not a role that happens to read less: it is
// scoped to a tenant, a closed date range and the named packs within it, is
// read-only, is time-boxed, and confers NO repository read (SPEC-0033). An
// auditor who could enumerate repositories has already learned something the
// grant did not authorize.
//
// What this surface encodes, and what it structurally excludes:
//
// - The grant carries scope and expiry only. It has no repository permission,
//   no role list that could imply one, and no field a caller could read as an
//   authorization outcome. Enforcement is a PDP decision (evidence.pack.read
//   gated by decision-time grant facts; auditor.grant.manage for this
//   surface), never a role toggle, a BFF check, or a token scope claim
//   (SPEC-0033 AC5).
// - Expiry is a server-recognized instant, not a timer contract: the server
//   reads the grant's state and expiry from Identity & Access at decision
//   time and supplies them as decision-context facts. A request can therefore
//   never carry grant state, an extension, or a renewal — there are no fields
//   to carry them in (SPEC-0033 AC8). Using a grant never moves its expiry;
//   widening one is a new CreateAuditorGrant decision.
// - Revocation is immediate by construction (SPEC-0033 AC7): the PEP supplies
//   grant state and expiry fresh on every decision request, so a revoked or
//   expired grant changes the decision's input and fails the very next
//   decision — there is no cached decision or token to outlive it.
//
// Every failure — a nonexistent, revoked, expired, cross-tenant, malformed or
// unauthorized grant — is the same coarse denial (SPEC-0001): an auditor must
// not be able to distinguish "does not exist" from "not yours", or probing
// this surface enumerates grants and tenants.

package identityv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AuditorGrantService_CreateAuditorGrant_FullMethodName = "/gitsaas.identity.v1.AuditorGrantService/CreateAuditorGrant"
	AuditorGrantService_RevokeAuditorGrant_FullMethodName = "/gitsaas.identity.v1.AuditorGrantService/RevokeAuditorGrant"
	AuditorGrantService_ListAuditorGrants_FullMethodName  = "/gitsaas.identity.v1.AuditorGrantService/ListAuditorGrants"
)

// AuditorGrantServiceClient is the client API for AuditorGrantService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type AuditorGrantServiceClient interface {
	// CreateAuditorGrant issues a scoped, read-only, time-boxed grant. The
	// caller supplies the auditor principal, the evidence scope (a closed date
	// range, an optional repository scope, the named packs) and a requested
	// expiry; the server assigns the grant's identity and version and may bound
	// the expiry — the issued expires_at is the fact that counts. Issuing is a
	// PDP decision (auditor.grant.manage, owner-only) and appends the immutable
	// audit record naming the granting admin and the auditor principal
	// (SPEC-0033 AC4). Idempotent per tenant and request ID: replaying a
	// request ID returns the same grant and appends nothing new.
	CreateAuditorGrant(ctx context.Context, in *CreateAuditorGrantRequest, opts ...grpc.CallOption) (*CreateAuditorGrantResponse, error)
	// RevokeAuditorGrant terminates a grant. Revocation takes effect on the
	// next decision, not the next cache cycle: grant state is read at decision
	// time and arrives as a decision-context fact (SPEC-0033 AC7). Revoking is
	// a PDP decision (auditor.grant.manage) and is audited. Not-found,
	// cross-tenant, already-revoked, expired and unauthorized are the same
	// coarse denial (SPEC-0001).
	RevokeAuditorGrant(ctx context.Context, in *RevokeAuditorGrantRequest, opts ...grpc.CallOption) (*RevokeAuditorGrantResponse, error)
	// ListAuditorGrants pages the tenant's grants for administration. It lists
	// scope, state and lifecycle only — never pack contents — and is a PDP
	// decision (auditor.grant.manage). A cross-tenant or unauthorized list is
	// the same coarse denial as an empty one (SPEC-0001).
	ListAuditorGrants(ctx context.Context, in *ListAuditorGrantsRequest, opts ...grpc.CallOption) (*ListAuditorGrantsResponse, error)
}

type auditorGrantServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAuditorGrantServiceClient(cc grpc.ClientConnInterface) AuditorGrantServiceClient {
	return &auditorGrantServiceClient{cc}
}

func (c *auditorGrantServiceClient) CreateAuditorGrant(ctx context.Context, in *CreateAuditorGrantRequest, opts ...grpc.CallOption) (*CreateAuditorGrantResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CreateAuditorGrantResponse)
	err := c.cc.Invoke(ctx, AuditorGrantService_CreateAuditorGrant_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *auditorGrantServiceClient) RevokeAuditorGrant(ctx context.Context, in *RevokeAuditorGrantRequest, opts ...grpc.CallOption) (*RevokeAuditorGrantResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RevokeAuditorGrantResponse)
	err := c.cc.Invoke(ctx, AuditorGrantService_RevokeAuditorGrant_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *auditorGrantServiceClient) ListAuditorGrants(ctx context.Context, in *ListAuditorGrantsRequest, opts ...grpc.CallOption) (*ListAuditorGrantsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListAuditorGrantsResponse)
	err := c.cc.Invoke(ctx, AuditorGrantService_ListAuditorGrants_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AuditorGrantServiceServer is the server API for AuditorGrantService service.
// All implementations must embed UnimplementedAuditorGrantServiceServer
// for forward compatibility.
type AuditorGrantServiceServer interface {
	// CreateAuditorGrant issues a scoped, read-only, time-boxed grant. The
	// caller supplies the auditor principal, the evidence scope (a closed date
	// range, an optional repository scope, the named packs) and a requested
	// expiry; the server assigns the grant's identity and version and may bound
	// the expiry — the issued expires_at is the fact that counts. Issuing is a
	// PDP decision (auditor.grant.manage, owner-only) and appends the immutable
	// audit record naming the granting admin and the auditor principal
	// (SPEC-0033 AC4). Idempotent per tenant and request ID: replaying a
	// request ID returns the same grant and appends nothing new.
	CreateAuditorGrant(context.Context, *CreateAuditorGrantRequest) (*CreateAuditorGrantResponse, error)
	// RevokeAuditorGrant terminates a grant. Revocation takes effect on the
	// next decision, not the next cache cycle: grant state is read at decision
	// time and arrives as a decision-context fact (SPEC-0033 AC7). Revoking is
	// a PDP decision (auditor.grant.manage) and is audited. Not-found,
	// cross-tenant, already-revoked, expired and unauthorized are the same
	// coarse denial (SPEC-0001).
	RevokeAuditorGrant(context.Context, *RevokeAuditorGrantRequest) (*RevokeAuditorGrantResponse, error)
	// ListAuditorGrants pages the tenant's grants for administration. It lists
	// scope, state and lifecycle only — never pack contents — and is a PDP
	// decision (auditor.grant.manage). A cross-tenant or unauthorized list is
	// the same coarse denial as an empty one (SPEC-0001).
	ListAuditorGrants(context.Context, *ListAuditorGrantsRequest) (*ListAuditorGrantsResponse, error)
	mustEmbedUnimplementedAuditorGrantServiceServer()
}

// UnimplementedAuditorGrantServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAuditorGrantServiceServer struct{}

func (UnimplementedAuditorGrantServiceServer) CreateAuditorGrant(context.Context, *CreateAuditorGrantRequest) (*CreateAuditorGrantResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method CreateAuditorGrant not implemented")
}
func (UnimplementedAuditorGrantServiceServer) RevokeAuditorGrant(context.Context, *RevokeAuditorGrantRequest) (*RevokeAuditorGrantResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method RevokeAuditorGrant not implemented")
}
func (UnimplementedAuditorGrantServiceServer) ListAuditorGrants(context.Context, *ListAuditorGrantsRequest) (*ListAuditorGrantsResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method ListAuditorGrants not implemented")
}
func (UnimplementedAuditorGrantServiceServer) mustEmbedUnimplementedAuditorGrantServiceServer() {}
func (UnimplementedAuditorGrantServiceServer) testEmbeddedByValue()                             {}

// UnsafeAuditorGrantServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AuditorGrantServiceServer will
// result in compilation errors.
type UnsafeAuditorGrantServiceServer interface {
	mustEmbedUnimplementedAuditorGrantServiceServer()
}

func RegisterAuditorGrantServiceServer(s grpc.ServiceRegistrar, srv AuditorGrantServiceServer) {
	// If the following call pancis, it indicates UnimplementedAuditorGrantServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AuditorGrantService_ServiceDesc, srv)
}

func _AuditorGrantService_CreateAuditorGrant_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateAuditorGrantRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuditorGrantServiceServer).CreateAuditorGrant(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuditorGrantService_CreateAuditorGrant_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuditorGrantServiceServer).CreateAuditorGrant(ctx, req.(*CreateAuditorGrantRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuditorGrantService_RevokeAuditorGrant_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeAuditorGrantRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuditorGrantServiceServer).RevokeAuditorGrant(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuditorGrantService_RevokeAuditorGrant_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuditorGrantServiceServer).RevokeAuditorGrant(ctx, req.(*RevokeAuditorGrantRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuditorGrantService_ListAuditorGrants_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListAuditorGrantsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuditorGrantServiceServer).ListAuditorGrants(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuditorGrantService_ListAuditorGrants_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuditorGrantServiceServer).ListAuditorGrants(ctx, req.(*ListAuditorGrantsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AuditorGrantService_ServiceDesc is the grpc.ServiceDesc for AuditorGrantService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AuditorGrantService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gitsaas.identity.v1.AuditorGrantService",
	HandlerType: (*AuditorGrantServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "CreateAuditorGrant",
			Handler:    _AuditorGrantService_CreateAuditorGrant_Handler,
		},
		{
			MethodName: "RevokeAuditorGrant",
			Handler:    _AuditorGrantService_RevokeAuditorGrant_Handler,
		},
		{
			MethodName: "ListAuditorGrants",
			Handler:    _AuditorGrantService_ListAuditorGrants_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "proto/identity/v1/auditor_grant.proto",
}
